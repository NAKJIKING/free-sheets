# 4단계 자체 악보인식 모델 — 진행일지

다른 세션이 이어받는 유일한 단서다. 단계마다 **결과·수치·다음 할 일**을 적는다.

---

## 2026-09-18 (집 PC 터미널 세션) — 1단계 데이터 공장 재설계 + 학습 코드 일습

### ⚠ 가장 먼저 알아야 할 것 — 코퍼스가 바뀌었다

사장님 지시 2건:
1. "악보생성은 하지말고 현재 라이브러리에 있는 것으로만 진행해줘"
2. "이미 라이브러리에 만곡이 넘는 곡이 있어~ 그걸 활용해줘"

그래서 **코퍼스를 자체 조판 .ly 에서 라이브러리 미디 전체로 바꿨다.**

계획서(`OMR_연구.md`)와 이 폴더의 옛 README 는 "우리 조판 원본 252곡" 을
전제하지만 **저장소에 실제로 있는 것은 `tools/original_src/all_out` 의
24곡 × 9악기(기본판 216 .ly) 뿐**이다. 나머지 곡의 `.ly` 는 커밋돼 있지
않다(`build_all.py` 가 `cfg*.json` 에서 만들어 내는 빌드 산출물이고,
저장소에는 PDF·미디만 커밋된다). **엘리제(`elise`)의 .ly 도 없다** —
관문 1 시험곡인데 `make_lines.py` 로는 만들 수 없었다.

대신 저장소에는 **미디 7,336개**가 있다. 이것이 저장소 안에서 자급 가능한
유일한 대규모 기호 데이터다(thesession ABC·mutopia .ly·PDMX MusicXML 원본은
수집 스크립트가 외부에서 받아 쓰고 커밋하지 않는다).

### 미디를 정답으로 써도 되는 이유 (실측)

연구문서의 "midi2ly 금지"는 **사람이 연주한** 미디에 대한 경고다. 라이브러리
미디는 abc2midi·MuseScore·LilyPond 가 찍은 것이라 격자에 정확히 붙어 있다.

| 항목 | 값 |
|---|---|
| 미디 파일 | 7,336 |
| 단선율 트랙 + 격자 정합 | **4,261곡 / 627,400음** |
| 16분 격자(Q=12 에서 3의 배수)에 든 음 | **91.3%** |
| 전음이 16분 격자인 곡 | 82.9% |
| 박자표 | 4/4 1771, 3/4 682, 6/8 571, 2/4 489, 9/8 101, 12/8 91, 2/2 90, 3/8 70, 3/2 57 … |
| 조표 | G 1037, D 806, C 685, Bb 281, F 272, A 208, Eb 204, Am 128 … |

**조 편중(다장조 80.6%)이라는 옛 약점이 사라졌다** — 라이브러리 조표가
이미 고르다. 특이 박자(3/2·9/8·12/8)도 들어 있는데, Audiveris 가 구조적으로
무너지던 바로 그 지점이다.

**정답이 어긋날 수 없는 구조**: 토큰 목록을 먼저 정하고 그것으로 `.ly` 를
써서 렌더하므로 이미지와 정답이 같은 원천이다. 그 위에 **LilyPond 가 같이 낸
미디를 다시 읽어 우리 토큰의 붙임줄 병합 음고열과 대조**한다(불일치면 그 줄을
버린다). `.ly` 작성 버그가 조용히 데이터에 섞이는 것을 막는 장치다.

### 한 일

**환경**
- LilyPond 2.24.4 설치 → `C:\Users\cocok\tools\lilypond-2.24.4\bin\lilypond.exe`
  (`LILYPOND` 환경변수로 지정한다. **윈도우판은 `.midi` 가 아니라 `.mid` 를 낸다** —
  `make_lines.py` 가 이것 때문에 렌더 성공을 전부 "실패"로 세고 있었다. 고침.)
- **GPU 있음: RTX 5070 Ti Laptop 12GB (sm_120)** → PyTorch 2.11.0+cu128 로 로컬 학습.
  **캐글 계정은 필요 없다.**
- free-sheets 를 blob 필터 + sparse-checkout 으로 클론
  (`C:\Users\cocok\free-sheets`, 전체 1.2GB 를 다 받지 않기 위해).
  받은 경로: `tools/omr_factory`, `tools/original_src`, `mids`, `abc`.

**새 도구 (전부 이 폴더에 커밋)**
| 파일 | 하는 일 |
|---|---|
| `lib_lines.py` | 미디 → 단선율 멜로디 → 마디·토큰 → LilyPond 한 줄 조판. 최소 미디 파서 포함 |
| `make_lib_lines.py` | 위를 7,336개에 돌리는 실행기(재개형, 정답 대조 내장) |
| `prep.py` | **오선 정규화** — 오선 5줄을 찾아 줄간격 12px·높이 160 으로 맞춘다 |
| `cache.py` | 300DPI 렌더를 전처리해 학습용 회색 PNG 로 굽고 `index.jsonl` 생성 |
| `dataset.py` | 어휘·증강(순서 고정)·`ThreadLoader` |
| `model.py` | CRNN(5블록) + BiLSTM 2층 + CTC |
| `train.py` | 학습(재개 가능, 에폭마다 검증 NER) |
| `split.py` | **곡 단위** 학습/검증/시험 분할 |
| `evaluate.py` | 관문 1 판정 + 엘리제 예측·정답 미디 생성 |

**받아들이는 조건** (하나라도 어긋나면 그 청크를 버린다)
- 단선율 트랙(겹침 없음), 음 16개 이상
- 모든 온셋·길이가 16분 격자 → **셋잇단(4·8틱)은 이번 판에서 제외**
- 박자표가 실제 악보에 쓰는 것(`lib_lines.TIMESIGS`) — abc2midi 가 내는
  1/8·1/4·1/2 같은 퇴화 박자표는 8분음표마다 마디줄이 그어져 비현실
- 조옮김 결과 조표가 ±7 이내(`key_ok`) — 플랫 8개짜리 악보 방지

**이번에 잡은 결함 5건** (다음 세션이 같은 함정에 빠지지 않게)
1. 윈도우 LilyPond 는 `.mid` — `.midi` 만 찾으면 전부 실패로 센다.
2. `mido` 는 LilyPond 가 먼 조로 옮길 때 내는 조표(플랫 8개↑)에
   `KeySignatureError` 를 던진다 → **메타를 해석하지 않는 최소 미디 파서**로 교체.
3. **재개(skip) 할 때 정답 대조를 건너뛰면 안 된다** — 지난 실행에서
   불일치로 버린 파일이 다음 실행에서 조용히 통과해 데이터에 섞인다.
4. **검증셋이 비면 NER 0.0 이 나와 "최고 기록"으로 저장된다** → `None` 반환 + 경고.
5. **시험에서 어휘 밖 토큰이 있는 줄을 버리면 점수가 부풀려진다** →
   버리지 않고 −1 로 남겨 오류로 센다(`load_rows(keep_unk=True)`).
   그리고 같은 곡의 악기 판본 9개가 학습·시험에 갈라지지 않도록
   `song_key()` 가 악기 폴더를 떼고 묶는다.

**환경 함정**: 이 PC 의 실행 환경에서 **torch `DataLoader` 의 워커 프로세스가
`PermissionError: [WinError 5]`(큐 세마포어) 로 죽는다.** 그래서
`dataset.ThreadLoader`(스레드 기반)로 대체했다. `num_workers` 를 쓰는 코드를
다시 넣지 말 것.

### 수치

- 렌더 작업 **43,533건** / 곡 탈락 4,028
  (단선율없음·격자밖 3,072, 마디화실패 678, 박자표제외 278)
- 진행 중 실패 **0건**, 정답 대조 불일치 **0건**
- 엘리제: 9악기 전부 통과, 3/8 8마디 한 줄(토큰 37개)로 조판됨 — 육안 확인 완료

### 다음에 할 일 (순서대로)

```bash
cd C:/Users/cocok/free-sheets
export LILYPOND='C:/Users/cocok/tools/lilypond-2.24.4/bin/lilypond.exe'

# 1) 생성 끝까지 (재개형 — 다시 돌려도 이어서 한다)
python tools/omr_factory/make_lib_lines.py --out C:/Users/cocok/omr_lines --keys 3 --max-chunks 6

# 2) 곡 단위 분할 (엘리제는 무조건 시험용)
python tools/omr_factory/split.py --data C:/Users/cocok/omr_lines

# 3) 전처리 캐시
python tools/omr_factory/cache.py --data C:/Users/cocok/omr_lines

# 4) 학습 (GPU)
python tools/omr_factory/train.py --data C:/Users/cocok/omr_lines \
    --out C:/Users/cocok/omr_model --epochs 40 --batch 16 --aug 0.35 --workers 8

# 5) 관문 1 판정 + 엘리제 증명물
python tools/omr_factory/evaluate.py --data C:/Users/cocok/omr_lines \
    --model C:/Users/cocok/omr_model --out C:/Users/cocok/omr_gate1
```

관문 1 = 시험용 곡(학습에 안 쓴 곡)의 깨끗한 렌더에서 **음표 NER 5% 이하**.

### 아직 안 한 것 / 알려진 한계

- **셋잇단 제외** — 음표의 8.5%(4틱·8틱). 아일랜드 전통곡에 많다.
  관문 1 통과 후 `\tuplet` 쓰기를 넣을 것.
- **증강이 전처리 뒤(줄간격 12px)에서 돈다** — 순서(기하→잉크→종이결→조명→
  광학→센서→다운샘플/JPEG)는 지켰지만 300DPI 원본 위가 아니다. 실사 사진
  대응은 2단계 과제이고, 원본 렌더는 `C:\Users\cocok\omr_lines` 에 그대로 있다.
- 화음·2단 보표 없음(설계대로 단선율만).
- 렌더러 1종(LilyPond)·악보 폰트 1종. 연구문서의 "렌더러 2종 + 폰트 3종"은 미적용.

---

## 2026-09-19 (웹 세션) — 🔴 결함 수정: 조옮김 라벨이 이미지와 어긋남

make_lib_lines.py 가 토큰을 **조옮김 전** 음으로 저장하고 정답 대조식에서만
시프트를 더했다 → 자체 검사는 통과하지만 cache/dataset/evaluate 는 토큰을
그대로 쓰므로 **시프트≠0 표본 전부(keys=3 이면 2/3)가 오답 라벨**이었다.
이대로 학습했으면 관문 1이 원인 불명으로 미달했을 것이다.

수정: 토큰을 '이미지에 보이는 음'(조옮김 후)으로 저장. 웹 세션 검증:
12/12 토큰=렌더미디 일치, 조옮김 관계(kp0+6=kp6) 정확.

**이미 생성한 출력 폴더는 재렌더 불필요** — 같은 --out 으로 생성기를 다시
돌리면 manifest.jsonl 이 처음부터 다시 쓰이며 고쳐진다(skip 경로도 토큰을
재계산·재대조한다. 43,533건 기준 렌더 없이 대조만 하므로 수십 분).
⚠ 이 수정 전 manifest 로 cache.py 를 이미 돌렸다면 index.jsonl 도 다시
구울 것(cache 는 manifest 토큰을 복사한다).

---

## 2026-09-19 (식당 PC 터미널 세션, `user`@DESKTOP-H55UD16) — 관문 1까지 논스톱 시도

웹 세션 지시: fcf4c5bd 이상으로 pull 후 "다음에 할 일" ①~⑤ 를 순서대로.
**이 PC 에는 아무것도 없었다** — 클론·LilyPond·torch·이전 생성물 전부 없음.
집 PC 의 43,533건 재사용은 불가, 처음부터 생성한다.

### 이 PC 환경 (집 PC 와 다른 점)

| 항목 | 집 PC (cocok) | 식당 PC (user) |
|---|---|---|
| GPU | RTX 5070 Ti Laptop 12GB | **RTX 5060 8GB** (sm_120, 드라이버 610.88) |
| CPU 코어 | — (2코어 제한) | 12 (제한 해제 — 블루스크린 이력 없음) |
| C: 여유 | — | 시작 21GB → 클론·sparse 후 19GB. **5GB 아래로 가면 멈추고 보고** |
| LilyPond | `C:\Users\cocok\tools\lilypond-2.24.4\bin\lilypond.exe` | winget `LilyPond.LilyPond` 2.24.4 → `C:\Users\user\AppData\Local\Microsoft\WinGet\Packages\LilyPond.LilyPond_Microsoft.Winget.Source_8wekyb3d8bbwe\lilypond-2.24.4\bin\lilypond.exe` |
| 클론 | blob 필터 + sparse | 전체 클론 3.1GB 후 `git sparse-checkout set --no-cone tools mids abc catalog.json` 으로 `raw/`(1.4GB)·`thumbs`·`keys` 제외 |
| Python | — | uv venv `free-sheets/.venv` (3.12.14) + torch cu128 + numpy + pillow + mido |
| 산출 경로 | `C:\Users\cocok\omr_lines` 등 | `C:\Users\user\omr_lines` / `omr_model` / `omr_gate1` |

- Claude Code 2.1.278 최신 — 재시작 불필요 (긴 작업 전 확인함).
- LilyPond 단독 렌더 실측: 4마디 한 줄 → PNG 2013x313 RGB **23KB**, .mid 0.3KB, 첫 렌더 7.3초(폰트 캐시 포함).
  → 43,533건 ≈ **1.1GB**. 디스크 예산 안.

### 연기 시험 (본 생성 전에 이 PC 에서 전 단계가 도는지 확인)

- 생성기 `--only elise`: 9악기 × 3조 = **27건 OK, 실패 0**, 12.6초(6병렬). 건당 ≈40KB.
- 생성기 `--limit 400 --max-chunks 3`: 1,034건 OK / 실패 0 / 대조불일치 0 (스크래치 폴더).
- 그 1,034건으로 **split → cache → train 1에폭 → evaluate 전부 통과**:
  캐시 탈락 0(오선못찾음 0), 어휘 346종, GPU 학습 854줄 66초(배치 16, `--workers 4`),
  OOM 없음, gate1.json 생성. (1에폭이라 NER 100% — 값은 의미 없음, 배관 확인용.)
- `ThreadLoader` 는 이 PC 에서도 그대로 씀. `DataLoader` 재도입 안 함.
- ⚠ 콘솔 출력이 깨지면 `PYTHONUTF8=1` — 스크립트가 한글을 찍는데 cp949 콘솔이라 mojibake.

### ① 생성 (진행 중)

- 14:15:23 시작, 계획 단계 4분(7,336 미디 파싱, 단일 프로세스).
- **렌더 작업 43,533건 / 곡 탈락 4,028 — 집 PC 와 수치 완전 일치.**
- 12병렬 실측 3.4장/초 → 약 3.5시간, **17:50 완료 예상**. 디스크 ≈1GB.
- 명령: `PYTHONUTF8=1 LILYPOND=<위 경로> .venv/Scripts/python.exe tools/omr_factory/make_lib_lines.py --out C:/Users/user/omr_lines --keys 3 --max-chunks 6 --jobs 12` (로그 `C:\Users\user\omr_gen.log`)
- 디스크 함정: **uv 캐시가 `%LOCALAPPDATA%\uv\cache` 에 10GB** 쌓여 있었다(torch 휠). `uv cache clean` 으로 회수.
- ⚠ **15:03 생성 프로세스 강제 종료됨 — 블루스크린 아님.** Claude Code 하네스가 "시스템 메모리 부족"으로
  백그라운드 작업을 죽였다(이 PC RAM 16GB, 유휴 시 여유 4GB. LilyPond 12개가 PNG 변환에서 동시에
  치솟음). 10,440장에서 멈춤. 15:07 `--jobs 8` 로 재개 — 하네스 감시 밖 독립 프로세스
  (`C:\Users\user\omr_gen.cmd`, `Start-Process`)로 띄워 같은 이유로 다시 죽지 않게 함. 로그 `omr_gen2.log`.
  크래시 횟수: **0회(블루스크린)** / 하네스 메모리 킬 1회.
- ⚠ 디스크 급감(15→9GB, 16:35)은 렌더가 아니라 **pagefile.sys 가 21GB 로 팽창**한 것
  (커밋 25.4/30.2GB — ChatGPT 앱 4개·claude·codex 등 상주 앱 누적). 렌더 산출물은 585MB 뿐.
  일회성 확장이라 판단, 6GB 경고·5GB 정지 장치 유지한 채 속행. 재부팅하면 회수된다.

### ① 생성 완료 + ② 분할 (17:33 / 17:36)

- **43,533 / 43,533 전부 성공** (skip 10,440 + ok 33,093), 렌더실패 0 · 대조불일치 0 · manifest 43,533줄.
  집 PC 계획 수치와 완전 일치. 산출물 ≈0.9GB, 총 소요 ~3.3시간(중단 1회 포함).
- ② 곡 단위 분할: **train 2,031곡/36,109줄 · val 243곡/3,947줄 · test 195곡/3,477줄.**
  엘리제 = test, 27줄(9악기×3조) 확인.
- ③ 캐시(8병렬) → ④ 학습(40에폭, 배치 16) → ⑤ 평가는 체인 스크립트
  `C:\Users\user\omr_stage2.cmd` 가 자동 진행(로그 `omr_pipeline.log`).
  ⚠ 학습 예상 시간: 스모크 실측 1.24초/스텝 기준 **에폭당 ~47분, 40에폭 ≈ 31시간**.
  체크포인트 재개형이므로 끊겨도 `--resume` 으로 이어진다.

### ④ 학습 1차 시도 — 무음 크래시 1건과 그 방어 (17:42~17:46)

- 첫 실행(배치 16, 워커 8)이 데이터 로드 출력 직후 **에폭 0 시작 전에 소리 없이 죽음**
  (트레이스백 없음, 종료코드 음수로 추정). 같은 순간 하네스도 "메모리 부족"으로 다른 작업을
  죽인 것으로 보아 **시스템 커밋 고갈(24.9~25.4/30GB, 상주 앱 다수)** 시점의 네이티브 크래시로 판단.
- ⚠ **cmd 함정: 네이티브 크래시의 음수 종료코드는 `if errorlevel 1` 에 안 걸린다** →
  실패한 학습을 성공으로 알고 stage5 로 넘어가 best.pt 없음으로 PIPELINE_FAIL.
  `if not "%errorlevel%"=="0"` 로 고침.
- 재시작: `C:\Users\user\omr_train.cmd` — 시도 사다리 (1~2차 배치16/워커4 →
  3~4차 배치8/워커2 → 5~6차 배치4/워커2), 매 시도 `--resume` 이라 체크포인트에서 이어짐.
  학습 데이터: 36,102줄 / 검증 3,946줄 / 어휘 1,123종.

### ④ 학습 2차 사고 — 커밋 비대 → 페이징 붕괴, 감독기 v2 로 교체 (18:48~19:21)

- 1차 재시작(배치 16, 워커 4)은 1,400스텝까지 1.1초/스텝으로 정상 → 이후 **학습 프로세스 커밋이
  16.8→18.3GB 로 계속 비대**(작업셋은 1.8GB뿐 — Windows 힙 단편화/가상메모리 누수 양상).
  페이지파일 33GB 팽창, 페이징 초당 18,322회, GPU 38W(사실상 대기), 스텝당 7초로 붕괴.
  에폭 0 체크포인트 저장 전이라 기다릴수록 손해 → 19:20 강제 종료(에폭 0 손실 ~1.7h).
- **감독기 v2**(`C:\Users\user\omr_train2.ps1`)로 교체: 배치 8·워커 2(메모리 친화),
  크래시·비대는 정상 사건으로 취급 — **에폭이 전진하는 한 무한 재시작**(`--resume`, 프로세스
  교체가 커밋을 리셋), 3회 연속 무진전 또는 디스크 5GB 미만이면 그때만 중단.
- ⚠ **PS5.1 함정**: PowerShell 의 `>>` 는 UTF-16 을 써서 UTF-8 로그를 깨뜨린다(감시 grep 도
  무력화). 파이썬 리다이렉트는 `cmd /c "... >> log 2>&1"` 로 우회.
- 크래시 집계: 블루스크린 0 / 학습 무음 크래시 1 / 페이징 붕괴로 인한 수동 교체 1.

### 🎯 관문 1 통과 — 에폭 3 모델 조기 판정 (2026-09-19 20:44, 식당 PC)

- 검증 NER 추이: 에폭0 43.6% → 1 22.6% → 2 8.3% → **3 4.26%** (에폭당 ~15분, 배치 8)
- **⑤ 조기 평가(시험셋 3,475줄·195곡, 학습 미사용): 음표 NER 2.71% ≤ 5% → 통과.**
  쉼표포함 2.64%, 무오류 줄 71.3%, 어휘밖 24토큰 오류로 계산. `C:\Users\user\omr_gate1_ep3\gate1.json`
- 엘리제 증명물 27쌍(9악기×3조 정답/모델 미디) 같은 폴더에 생성. 엘리제 합계는 972음 중 97오류
  = **9.98%** — 첼로·리코더의 낮은 조(km5·kp6 등)에서 오류 집중. 40에폭 최종 모델로 재평가 예정.
- 시험 오류율 상위: Mendelssohn O37-2 40.7%, ArbeauT belle 20.8%, BWV628/629 ~20% 등 —
  대체로 길고 조밀한 곡.

#### 이 PC 운영 수칙 (재발 방지, 오늘 실측으로 확정)
1. **학습·평가 동시 실행 금지** — 커밋 53GB → 페이지파일 38GB → C: 1GB 사태의 원인.
   순차 실행하면 커밋이 즉시 회수된다(프로세스 종료로 13~15GB 복구 확인).
2. train.py 에 `OMR_EPOCHS_PER_RUN`(기본 0=무제한) 추가 — 감독기가 2에폭마다 프로세스를
   교체해 윈도우 커밋 비대(에폭당 수 GB)를 리셋한다.
3. CPU 강제는 `CUDA_VISIBLE_DEVICES=-1` — 빈 문자열은 MSYS 가 스트립하고, 스트립돼도
   torch 가 is_available()=True / device_count()=0 인 어정쩡한 상태가 된다.
4. 평가는 GPU 가 빈 틈(학습 정지 중)에 돌리는 것이 가장 빠르고 안전했다(3,475줄 ~7분).

#### 다음 (관문 1 이후)
1. 학습 40에폭까지 계속(감독기 자동, ~9시간) → 최종 best.pt 로 ⑤ 재평가 → 증명물 갱신
2. 웹 세션: `omr_gate1_ep3\`(또는 최종 `omr_gate1\`) 의 미디 쌍으로 청음 비교 페이지 제작
3. 셋잇단(\tuplet) 확장, 실사 폰사진 증강은 관문 1 이후 과제로 유지
- 21:47 디스크 3GB 재경보: 커밋 비대가 예상(에폭당 ~5GB)보다 빨라 2에폭 주기로도 12GB 도달
  + 외부 java 프로세스 3GB 겹침. **OMR_EPOCHS_PER_RUN=1 로 단축** 후 재시작 → 여유 24GB 회복.
  에폭당 재시작 오버헤드 ~90초(10%). 검증 NER 추이: ep4 2.43% → 5 1.89% → 6 **1.23%**(best) → 7 1.52%.
- 22:10 감독기 배치 4 로 하향(`omr_train2.ps1`, 디스크 압박 완화). 에폭 9 완료(검증 NER 1.53%,
  best 유지 ep6 1.23%)까지 확인 후 **PC 전원 꺼짐** — 감독기 재시작 직전 시점이라 손실 없음.

---

## 2026-09-20 (식당 PC 웹 세션) — 정전 후 재개, 에폭 10부터 속행

- 전원 꺼짐 피해 점검: `last.pt`(ep9)·`best.pt`(ep6)·`train_log.jsonl`·`omr_gate1_ep3\`
  (gate1.json + 엘리제 27쌍) 전부 무사. 디스크 여유 34GB(재부팅으로 pagefile 회수).
  git 은 origin 과 동일(d52fed20), pull 불필요.
- 14:18 감독기 v2 재시작(배치 4·워커 2·EPOCHS_PER_RUN=1, 독립 프로세스) →
  에폭 10부터 `--resume` 정상 재개. 남은 30에폭 ≈ 10~13시간 예상(배치 4 기준).
- 관문 1 은 어제 20:44 에 이미 통과(시험 음표 NER 2.71%) — 이번 세션은 40에폭 완주 후
  최종 재평가·증명물 갱신이 목표.
- 에폭 10 검증 NER **0.59%**(신기록, 무오류 줄 93.8%) → 11 1.08% → 12 1.06%. 배치 4 는
  에폭당 ~20분. 검증이 240줄로 준 것은 배치 4 렌더 순서 차이(현상만 기록).
- 재부팅 후 디스크 여유 34→235GB 로 회복(pagefile·시스템 정리 추정). 5GB 경계 무관해짐.

### 🔶 15:40 일시 정지 + 식당컴2 인수인계 (사장님 지시: 이 PC 끄고 다른 PC 에서 재개)

- 사장님 지시로 감독기·학습 중지. **체크포인트는 에폭 12 까지 저장**(에폭 13 중반분만 손실).
- `omr_train2.ps1`(감독기 v2)을 `tools/omr_factory/`에 커밋함 — 단 경로가
  `C:\Users\user\...` 하드코딩이라 **새 PC 사용자명에 맞게 고쳐 쓸 것**.

**이 PC(user@DESKTOP-H55UD16)에만 있는 것 — git 에 없음 (데이터·모델 커밋 금지 규칙):**
| 경로 | 크기 | 내용 |
|---|---|---|
| `C:\Users\user\omr_lines` | 1.4GB | 렌더 43,533건 + manifest + split.json + cache(index.jsonl) |
| `C:\Users\user\omr_model` | 83MB | best.pt(ep6 기준 갱신, 검증 NER 0.59%는 ep10)·last.pt(ep12)·train_log.jsonl·vocab.json |
| `C:\Users\user\omr_gate1_ep3` | ~0.1MB | **관문 1 증명물** — gate1.json + 엘리제 정답/모델 미디 27쌍 |

**식당컴2 재개 경로 (둘 중 하나):**
1. **(권장) 위 3개 폴더를 USB/드라이브로 복사** → 클론 + `.venv`(uv, torch cu128) + LilyPond 설치 후
   감독기 재실행 → 에폭 13부터 이어짐. 총 준비 ~30분 + 남은 학습 ~9.5시간.
2. **복사 불가 시 처음부터**: 진행일지 2026-09-19 절차 그대로 — 생성 3.5h + 캐시 + 학습 0에폭부터
   (~14시간). 관문 1 증명물은 이 PC 에 남아 있으므로 재생성 불필요(최종 평가로 갱신 예정이었음).

⚠ 어느 쪽이든: DataLoader 금지(ThreadLoader), PS `>>` 금지(cmd 경유), PYTHONUTF8=1,
학습·평가 동시 실행 금지, 새 PC 사양에 맞춰 배치(8GB GPU=8~16, 메모리 압박 시 4) 조정.

---

## 2026-09-21 (식당 PC 웹 세션) — 40에폭 완주 + 관문 1 최종 통과 ✅

- 정정: "식당컴2 이관"은 오기 — 이 PC(user@DESKTOP-H55UD16)가 원래 식당컴이고 이관 없이 여기서 속행.
- 09-20 16:00 감독기 재시작(에폭 13부터 `--resume`) → 09-21 00:57 **40에폭(ep39) 완주**.
  알려진 크래시(0xC0000409)는 에폭마다 발생했으나 감독기가 27회 전부 자동 복구, 에폭 손실 0.
- 검증 NER 추이: ep12 1.06% → 14 0.49% → 16 0.34% → 26 **0.34%/완벽 95.0%**(best) → 30대 0.34~0.41% 안정 진동.
- **관문 1 최종 재평가(stage5, best=ep26) — 통과**:
  - 시험 세트 3,475줄 · 195곡 · 77,704음
  - **음표 NER 0.54%** / 쉼표 포함 0.52% (기준 ≤5% 의 1/9)
  - 무오류 줄 3,130/3,475 (**90.1%**), OOV 토큰 24
  - 엘리제 27변형(9악기×조옮김): **22곡 완전 무오류**, 총 오류 15/972음(1.54%).
    최다 오류는 cello km5 5음·km3 4음, 나머지는 0~2음.
  - 첫 평가(09-19, ep3) NER 2.71% → 최종 0.54% 로 5배 개선.
- 증명물: `C:\Users\user\omr_gate1\`(gate1.json + 엘리제 정답/모델 미디 27쌍). 구 증명물은 `omr_gate1_ep3\`에 보존.
- 다음 단계: 관문 2 착수 전 사장님 확인 대기.

---

## 2026-09-21 (식당 PC 웹 세션) — 증명물 공유 + 관문 2 착수

- 사장님 승인으로 "미디 커밋 금지" 예외 적용 → 관문 1 증명물(gate1.json + 엘리제 미디 27쌍, 94KB)을
  `tools/omr_factory/gate1_proof/`에 복사해 커밋. 대용량 데이터·모델·이미지는 계속 커밋 금지.
- 밤새 크래시 27회 원인 한 줄: **학습 프로세스의 커밋 메모리 비대(ThreadLoader·CUDA 워커 누적)로 인한
  네이티브 크래시 0xC0000409(STATUS_STACK_BUFFER_OVERRUN)** — 에폭 1회마다 프로세스를 교체해 커밋을
  리셋하는 감독기 설계로 흡수, 에폭 손실 0.

### 관문 2 ①단계 — 실사 파이프라인 구축 + 기준선 측정

- 새 코드: `prep.py`(사진용 국소 이진화·soft 오선찾기·normalize_photo — 관문 1 경로 불변),
  `dataset.py`(**300DPI 원본 위** 사진 증강 augment_photo: 원근→잉크→종이결→조명·그늘→광학→센서→
  다운샘플/JPEG, 마지막에 normalize_photo — 기존 12px 증강의 한계 해소),
  `photo_prep.py`(사진→기울기보정→빗살상관 오선계 검출→줄 크롭; 하모닉은 다섯 빗살 최솟값으로 차단),
  `mock_photos.py`(시험 분할 원본으로 모의 폰사진 생성, 페이지당 줄간격 17~24px 고정),
  `evaluate_photo.py`(끝단 평가 — 줄 정렬은 DP, 누락·유령 줄도 오류로 정직 채점),
  `train.py`(--photo-aug 혼합확률, --init 미세조정, 사진 검증 NER 로 best 선택).
- 모의 세트: 시험 분할 681줄 → 80페이지(`omr_photo_mock`, 시드 77). 이미지는 커밋 금지 유지.
- **기준선(관문 1 모델 그대로): 모의 사진 음표 NER 56.45%** (8,364/14,817음).
  줄 검출은 정답 681 중 검출 674(누락 19·유령 12) — 전처리는 건전, 인식이 실사 열화에 약함.
  열화 약한 페이지는 0.6%까지 나옴 → 증강 미세조정으로 좁힐 수 있는 간극.
- 다음: best.pt 에서 photo-aug 0.4 로 8에폭 미세조정(`omr_train_g2.ps1`, 감독기 v2 설계 재사용,
  출력 `omr_model_g2`) → 모의 재평가 + 깨끗한 렌더 회귀 검사.

### 관문 2 ②단계 — 사진 증강 미세조정 완료, 모의 사진 관문 통과 ✅

- 13:21~16:28 best.pt(관문1, ep26)에서 8에폭 미세조정(photo-aug 0.4, lr 1e-4, 배치 4).
  알려진 크래시 8회 전부 감독기 자동 복구. best 는 **ep4 (사진 검증 NER 1.39%)**.
- **모의 사진 80페이지 끝단 평가: 음표 NER 56.45% → 6.16%** (912/14,817음) — 관문 2 모의 기준(≤10%) 통과.
- 깨끗한 렌더 회귀 검사: NER 0.54→0.59%, 엘리제 무오류 22/27 유지 — **관문 1 여전히 통과**, 회귀 미미.
- 남은 약점은 인식이 아니라 **가혹 페이지의 줄 검출**(누락 19·유령 12줄; 최악 페이지들은 9줄 중 5~6줄만
  검출). 실물 50장 결과를 보고 줄별 재보정·검출 문턱 조정 여부를 정한다.
- 다음: 사장님께 실물 폰사진 50장 요청(촬영 요령 포함). 수령 후 실물 평가 → 미달 항목 보강.

### 관문 2 ③단계 — 보정 전처리 3종 추가, 가혹 모의에서 효과 실증

- 사장님 보완 지시대로 `photo_prep.py`에 보정 3종 추가 (numpy+PIL 만, cv2 무의존):
  ① `crop_paper` 종이 사각형 검출(밝은 마스크 대각 극점 4귀퉁이)→원근 보정.
     그늘진 종이를 배경으로 오인하는 사고가 v1 모의에서 실측돼(6.16→20.9% 악화)
     **테두리가 중앙보다 0.2 이상 어두울 때만**(진짜 배경 존재) 자르는 안전장치를 넣어 해소.
  ② `flatten_illum` 조명 평탄화 — 축소본 국소최댓값으로 종이 밝기를 추정해 나눗셈(이득 상한 방어).
  ③ `dewarp_line` 줄 단위 되펴기 — 세로 조각마다 빗살 상관으로 오선 중심을 재고
     2차 다항식 굴곡을 열별 세로 이동으로 상쇄(오선=직선 성질 이용).
- 모의 v2 생성(`omr_photo_mock2`, 80페이지·682줄, 시드 78): 휘어짐(sin 8~30px)+어두운 책상 배경
  +원근 배치 추가 — 보정이 되돌려야 할 열화를 전부 포함.
- **전/후 수치 (모델 omr_model_g2 동일)**:
  | 세트 | 보정 끔 | 보정 켬 |
  |---|---|---|
  | v1 (배경·휘어짐 없음) | 6.16% | **5.93%** (누락 19→14) |
  | v2 (배경+원근+휘어짐) | 8.37% (누락 31·유령 47) | **2.63%** (누락 1·유령 7) |
  가혹 조건일수록 보정 효과가 큼 — v2 에서 오류 2/3 감소, 줄 검출 사실상 완전 복구.
- 처리 속도 ~1.6초/페이지(보정 포함) — 실물 50장 평가에 문제 없음.
- 실물 50장 수령 시 보정 켬/끔 두 성적을 모두 산출 예정(지시 반영).

### 관문 2 ④단계 — 실물 폰사진 1차 수령·분석: 채점 불가 판정(곡 불일치), 파이프라인은 실물 검증됨

- 실물 사진 수령: `H:\내 드라이브\악보사진` 55장 안내 → **실제 54장**, `C:\Users\user\omr_photo_real`로 복사.
- 실물 대응 수정 2건: EXIF 회전 적용(폰사진 orientation=6), 펼친 책의 옆페이지 조각·책모서리 제거
  (줄 크롭을 '오선 잉크 최장 연속 구간'으로 제한).
- **판정: 자동 매칭 채점 불가.** 54장 전부 동일한 **플루트 교본**(Chapter 4 플루트 연주해보기,
  파헬벨 〈캐논 변주곡〉 플루트 2중주, 높은음자리 2단)을 조명·배경만 바꿔 반복 촬영한 것 —
  라이브러리 24곡 목록에 캐논이 없어 **정답 부재**. 인쇄 목록 10곡의 사진이 아님.
  evaluate_real.py(음정렬 조옮김 불변 매칭 + infix 정렬)로 전 곡 대조: 유의미 매칭 0곡,
  54장 전원 '애매' 목록 — 지시대로 별도 보고.
- 정답 없이 가능한 실물 검증(줄 검출 안정성, 페이지당 보표 ~10개):
  | | 검출 보표 수 중앙값 | 정확히 10개 | 완전 실패(0개) |
  |---|---|---|---|
  | 보정 끔 | 4 | 1/54 | 5/54 |
  | 보정 켬 | **10** | **30/54** | 0/54 |
  → 모의에서 확인한 보정 효과가 **실물에서 재현**됨. 인식 채점만 정답 있는 사진이 필요.
- 다음: 사장님께 재촬영 요청 — **인쇄한 10곡(우리 라이브러리 곡) 낱장**을 같은 요령으로.
  (또는 교본 곡을 채점하려면 해당 곡 정답 악보 확보 필요 — 저작권 있는 상용 교본이라 비권장.)

### 관문 2 ⑤단계 — 캐논 2중주 실물 53장 평가 (1차): 좋은 사진은 통과권, 병목은 중간화질 인식

- 시험지 준비: 라이브러리에 꽉 찬 고급 악보가 없어 **직접 조판** — ①고급연습모음(8곡 425음),
  ②사장님 요청으로 교체한 **캐논(파헬벨) 2중주**(A4 꽉 참, 7단×2보표, 438음, `omr_dense/`).
  멜로디를 데이터로 정의해 조판 → 정답 토큰이 악보와 원천 동일(`truth_canon.json`).
- 실물 사진 53장 수령(안내 54장, 실제 53장; `omr_photo_real`→교본, `omr_photo_canon`→캐논).
- 평가기 `evaluate_duet.py`: 깨끗한 렌더를 모델로 읽어(오류 0.3%) **줄별 정답 14줄을 역산**
  (성부는 조판 구조로 — 돌림곡은 내용 판별 불가함을 실측), 사진 보표를 순서 보존 DP 로 정렬 채점.
  짝짓기 방식은 보표 1개 누락에 사진 전체가 무너져 폐기(실측 기록).
- 검출 강화 4건(전부 실측 기반): 옆 물체(스티커) 가로채기 방지 연결성분 필터(400px 플러드),
  전단 quad 기각, **응답 최대 오선계 기준 간격 필터**(16분음표 빔 오검이 중앙값을 뒤집는 사고 해소),
  좌/우 반쪽 병합 검출 + **오선 기반 원근 상쇄(rectify_by_staves)**. 14보표 완검출 17→28장.
- **1차 성적 (모델 omr_model_g2)**:
  | | 전체 NER | 중앙값 | ≤10% 사진 | 추출 보표(742 기대) |
  |---|---|---|---|---|
  | 보정 끔 | 36.5% | 27.1% | 10/53 | 534 (1성부 266·2성부 268) |
  | 보정 켬 | 38.4% | 32.1% | 9/53 | **664** (1성부 329·2성부 335) |
  최고 사진 0.23~1.2%(통과권), 14보표 완검출 26장도 NER 0.5~47%로 갈림.
- **진단**: 검출은 보정으로 88~90%까지 회복. 병목은 (a) 보정 워프 3중 리샘플링으로 크롭이 물러져
  인식 저하(끔이 총점 근소 우위인 이유), (b) 중간 화질(빛번짐·미세 흔들림) 사진의 인식 자체.
- 증명물: 대표 3장(최고·중앙·최악) 성부별 정답/모델 미디 12개 → `omr_gate2_canon/` + 구글드라이브
  `악보사진/증명물_캐논/`.
- 다음(야간): 리샘플 체인을 흉내내는 증강 추가 + 더 강한 세기로 **2차 미세조정(omr_model_g3)**
  → 아침에 캐논 재평가.

### 관문 2 ⑥단계 — 기준선 비교: 우리 모델 vs Audiveris 5.11 (캐논 실물 53장)

- 설치: winget `audiveris.org.Audiveris 5.11.0`(공식 MSI). 입력은 EXIF 회전만 적용한
  원본 JPG(`omr_av_in`) — 우리 보정 파이프라인은 안 태움(엔진 자체 성능 비교).
- 채점: `tools/omr_bench/score_canon_av.py` — 같은 지표((음높이,길이) 음표열 편집거리 NER,
  붙임줄 병합·쉼표 제외), 악장 분할 이어붙임, 파트→성부 최적 배정, 출력 없는 사진은 100% 오류.
- **결과 (전체 23,108음):**
  | | 전체 NER | 중앙값 | 비고 |
  |---|---|---|---|
  | 우리 모델(보정 끔) | 36.5% | 27.1% | |
  | 우리 모델(보정 켬) | 38.4% | 32.1% | 보표 추출 89.5% |
  | **Audiveris 5.11** | **91.7%** | **100%** | **31/53장 출력 실패** |
  - Audiveris 실패 원인(로그): 오선 14개 검출까지는 성공하나 리듬 처리에서
    `NullPointerException (measure is null)` → export 중단. 살아남은 22장도 최고 53.9%.
  - **사진별 승패: 53/53 전부 우리 모델 우세.** 폰사진(원근·그늘·손떨림)에서는
    스캔용으로 설계된 Audiveris 가 구조적으로 무너짐 — omr_bench 기존 실측
    (깨끗한 스캔에선 Audiveris 무오류)과 상보적인 그림.
  - 사진별 53행 전체 표: `omr_av_out/compare_table.md` (데이터 폴더, 커밋 제외 규칙).

### 관문 2 ⑦단계 — 2차 미세조정(g3) + 신뢰도 자동 선택: 실물 38.4→26.1%

- g3 미세조정(19:54~22:24, 6에폭): 리샘플 체인 증강 추가 + photo-aug 0.5·세기 1.25, g2 best 에서
  lr 7e-5. best=ep3, **사진 검증 NER 0.62%**(g2 1.39%의 절반 이하). 크래시 자동복구 6회.
- 켬/끔이 사진마다 승자가 갈리는 것을 실측(오라클 상한 22.8%) → **CTC 신뢰도(프레임별 최대
  소프트맥스 평균) 기반 켬/끔 자동 선택**을 evaluate_duet --auto 로 구현. 켬 20·끔 33 채택.
- **캐논 실물 53장 성적 추이 (전체 NER / 중앙값 / ≤10% 사진):**
  | 단계 | 전체 | 중앙값 | ≤10% |
  |---|---|---|---|
  | g2·보정 켬 (어제 밤) | 38.4% | 32.1% | 9장 |
  | g3·보정 끔 | 31.8% | 20.2% | 15장 |
  | **g3·자동 선택** | **26.1%** | **17.9%** | **17장** |
  | (오라클 상한) | 22.8% | 17.0% | 19장 |
  | Audiveris 5.11 | 91.7% | 100% | 0장 |
- 회귀 검사: 렌더 NER 0.57%(관문 1 통과 유지), 모의 v2 9.21%(기준 10% 통과 — g2 의 2.63%보다
  느슨해짐: 증강 분포가 실물 쪽으로 이동한 대가, 실물이 우선이므로 수용).
- 남은 간극(26→10%)의 주범: 보표 누락 사진(7~12개 검출)과 중간화질 인식. 다음 후보:
  줄별 재검출(누락 y 구간 재탐색), 흔들림(모션블러) 증강, 크롭 앙상블.

---

## 2026-09-22 (식당 PC 웹 세션) — 관문 2 속행: 누락 재탐색 + 모션블러 미세조정

- ① **보표 누락 재탐색**(`photo_prep.fill_missing`): 검출 보표 간격의 중앙값 대비 1.7배를
  넘는 '구멍'과 페이지 위·아래 여백만 문턱을 낮춰(tap 0.07·floor 0.10) 국소 재검출.
  낮춘 문턱이 구멍 안에만 적용되므로 빔 오검 부활 없음, 채택은 기준 간격 ±38% 검사.
- **캐논 실물 53장 (g3·자동선택): 26.1 → 23.1%** (중앙값 17.9→16.5%, ≤10% 17→19장)
  — 어제 켬/끔 오라클 상한(22.8%)을 검출 개선으로 추월.
- ② 손떨림(모션블러) 증강 추가(방향성 흐림, 확률 0.35) → **g4 미세조정 시작**(13:46,
  g3 best 에서 lr 5e-5·5에폭, `omr_train_g4.ps1`). 완료 후 4종 평가 자동.
- 남은 특이 사진: 190157(원거리, 정보량 자체 부족)·190127(12보표인데 77%) — g4 후 재진단.

### 관문 2 ⑨단계 — 검출 견고화 3건: 실물 23.1 → 21.7% (중앙값 12.8%)

- 실측 기반 수정 3건:
  ① 기울기 탐색 ±6°→±20°(190127 이 ~8° 회전인데 캡에 걸림), 큰 각은 expand=True 로 모서리 보존.
  ② rectify_by_staves 가드 — 좌/우 짝 어긋난 전단 워프가 이미지를 망가뜨리던 사고(12→3보표)
    차단: 잔차 1.5px 초과·보정량 12px 초과면 워프 포기, 짝 5개 미만이면 상수 전단만.
  ③ 원거리용 2패스 고해상도 재검출이 더 적게 찾으면 원래 결과 유지.
- **캐논 실물 (g3·자동선택): 21.69% / 중앙값 12.84% / ≤10% 사진 21장** (아침 26.1%에서 출발).
- g4(모션블러) 미세조정 병행 중 — 완료 시 이 검출 개선 위에 얹힘.

### 관문 2 ⑩단계 — g4(모션블러) 완료: 실물 19.8% / 중앙값 11.0% / ≤10% 24장

- g4 미세조정(13:46~15:44, 5에폭, g3 best 에서 lr 5e-5): best=ep1, 사진 검증 NER 1.31%
  (모션블러 포함 검증 기준). 크래시 자동복구 5회.
- **캐논 실물 53장 (g4·자동선택): 전체 19.81% / 중앙값 11.01% / ≤10% 사진 24장**
  — 오늘 하루 26.1→19.8%(전체), 17.9→11.0%(중앙값), 17→24장(≤10%).
- 회귀: 렌더 0.58%(관문 1 통과)·모의 v2 8.89%(통과) — 실물 개선이 공짜가 아님을 매번 검사.
- 남은 오류의 지배 요인: 극한 사진 소수(원거리 190157 99%, 회전+그늘 190127(0) 87%,
  190156 72% 등). 중간 화질 대부분은 이제 한 자릿수 NER.
- 다음 후보: 실패 상위 사진 유형별 대응(원거리 초해상, 극한 회전 재보정),
  줄 단위 크롭 앙상블, 또는 '촬영 가이드'(재촬영 안내)로 운영 우회 — 사장님 판단 필요.

---

## 2026-09-23 (식당 PC 웹 세션) — 관문 2 ⑪: 극한 유형별 대응(1번 과제) 결산

- 실패 14장(NER>20%)을 부류로 나눠 대응: **A 검출 부족 / B 검출됐는데 인식 붕괴**.
- 반영 4건(유지): ① 중복 검출 제거 임계 2.5→5.5칸(190122 17→14보표, 62→43%)
  ② x-트림을 '최장 run + 3×틈 이웃 병합'으로(조각난 줄 구제, 배경 쓸어담기 방지)
  ③ **normalize 에 페이지 검출 줄간격 힌트 전달**(크롭 재측정 배율 붕괴 사고 방지, 190158 줄13)
  ④ comb 중심 탐색을 크롭 중앙 ±2.5칸으로 제한(이웃 보표 가로채기 방지).
- 폐기 2건(실측 순손실): 어두운픽셀 비율 가드(16분 밀집 줄 오살, 39%), 가장자리 밝기 가드(22.3%).
- **원거리 사진 한계 규명(중요)**: 190158 줄별 대조 — 앞줄들은 정답과 거의 일치하나
  16분 밀집 줄에서 **음고는 맞고 길이(duration)를 오독**(줄10: 47/48 오류), 신뢰도는 0.98.
  줄간격 10px 의 정보 한계로 판단. 2x 슈퍼샘플 실험도 무효(±2%p).
- **최종 성적: 전체 19.98% / 중앙값 11.01% / ≤10% 22장 / ≤15% 33장** — 어제(19.81/11.01/24)와
  동률. 오늘 가치는 총점이 아니라 **특정 사고 유형 제거 + 파이프라인 무결성**(중복·배율·크롭 오염).
- 남은 자동선택 약점: 오염 크롭이 신뢰도 0.99로 오답을 내면 켬/끔 선택이 속는다(190139(0)) —
  진실 없는 선택 신호로는 미해결. **권고 유지: 촬영 가이드(원거리·극한각 재촬영 안내) 정책 병행.**
- 다음: 사용자 지시로 **관문 3-0 진입** (로드맵_관문3.md).

### 관문 3-0 — 줄 검출 누락·유령 ≤3% 도전 (진행 기록, 부분 달성)

- **격자 외삽 재탐색(slot_fill)** 신설: 검출 보표의 교대 주기(쌍내 s1·시스템간 s2)를 추정해
  빈 슬롯 ±2칸만 초저문턱 탐색. 주기 산포 15% 초과 시 포기(극회전 폭주 방지 게이트 — 실측 7→22보표 사고).
  효과: 190153(0) 7→13, 190142 8→14, 190127(0) 7→11 보표.
- 자동선택 기준 3종 A/B (53장 전수, 모두 실측):
  | 기준 | 실물 NER | 출력단 누락+유령 |
  |---|---|---|
  | 신뢰도(기존) | **19.64%** | 8.4% |
  | 상호 일치도 게이트 | 25.69% | 17.4% | (폐기)
  | **커버리지 우선** | 22.88% | **5.0%** |
  → 용도별 이중 모드로 코드화: `--auto`(NER 최적) / `--auto-cov`(3-0 줄 회수 최적).
- **판정 현황**: 검출 단계 누락은 12줄(1.6%)로 기준 안. 그러나 출력단(빈 디코드 제거 후) 기준
  5.0%로 **기준 3% 미달**. 잔여 블로커 = 어려운 크롭(원거리 흐림·저조도)에서 모델이 공백을
  출력(49줄 유형) + 오염 크롭을 신뢰도가 걸러내지 못하는 선택 신호 부재(190139(0) conf 0.99 오답).
- **막힘 보고(로드맵 규칙)**: 남은 간극은 전처리가 아니라 인식 모델·선택 신호의 한계로 판단.
  후보: (a) 저품질 크롭 전용 증강 5차 미세조정 (b) 공백 출력 크롭 재시도 체인(다른 변형 크롭 대입)
  (c) 3-0 기준을 '검출 단계'로 해석 시 현재 1.6%로 통과. **사장님 판정 요청 후 3a 진행 예정.**

### 관문 3a ① — 셋잇단 조판·토큰 수용 (lib_lines)

- `to_bars`: 16분 격자에 더해 **순수 셋잇단 창**(박 12틱 안에 온전히 든 4·8틱, 4틱 격자 정렬)
  수용. 박 경계를 걸치거나 직선과 섞인 비격자는 여전히 제외(보수 설계 — 직선 곡 출력은 불변).
- `bars_to_ly`: 셋잇단 창을 `\tuplet 3/2 { }` 로 조판, 토큰은 실제 틱(4·8) 그대로.
- 스모크: 변환 4케이스 + LilyPond 실렌더 — 셋잇단 괄호 정상, 미디 음고 = merged_pitches 일치.
- 이스케이프 함정 기록: 문자열 치환 두 겹에서 `\tuplet` 이 탭이 되거나 `\tuplet` 이 됨 — repr 로 확인할 것.
- 다음: 코퍼스 재생성(`omr_lines3a`, 기존 설정 --keys 3 --max-chunks 6) → cache → g4 best 미세조정 → 판정(셋잇단 시험셋 ≤5%) + 관문 1·2 회귀.

### 관문 3a ③ — 코퍼스·미세조정·판정 (2026-09-23 새벽)

- 코퍼스 재생성 `omr_lines3a`: 렌더 50,828/50,859 (실패 31) → 캐시 50,817줄 (**구판 +7,294줄 = 셋잇단 재수용 +16.8%**).
  분할은 곡명 SHA1 결정적이라 기존 곡 버킷 그대로(누수 없음). 시험 225곡 3,953줄(엘리제 27 포함).
- 미세조정 `omr_model_3a` (02:47~06:29, 8에폭): **--init-partial** 로 g4 이식 — 어휘 1,218종
  (공유 1,123행 이식 + 셋잇단 신규 95행 초기화). best=ep6.
- **관문 3a 판정: 통과** — 셋잇단 포함 시험셋 NER **0.51%** (기준 ≤5%), 무오류 90.8%.
  셋잇단 라인만(376줄·11,292음): **NER 0.48% / 무오류 91.5%** — 신규 토큰 완전 정착.
- 회귀: **관문 1 통과·개선**(0.58→**0.53%**, 엘리제 22/27 유지). **관문 2 후퇴**: 캐논 실물
  19.64→21.85% (-2.2%p, last.pt 도 동일) — 새 데이터 분포로 사진 강건성 일부 망각.
- 로드맵 회귀 규정 대응: **3b 폴리싱** 시작(06:48) — 3a best 에서 photo-aug 0.6·lr 4e-5·3에폭,
  완료 시 판정·회귀 3종 자동 재실행. 회복되면 3a 확정, 안 되면 사장님 판정(폐기 vs 수용).

### 관문 3a ④ — 3b 폴리싱 결과와 판정 요청 (08:24)

- 3b(photo-aug 0.6·lr 4e-5·3에폭, best=ep1 사진검증 1.18% 신기록): 셋잇단 판정 0.51% 통과 유지,
  관문 1 0.53% 유지. **캐논 실물 21.86% — 회복 실패**(3a 21.85%와 동일). 사진 '검증'(우리 증강
  분포)과 실물 캐논 분포가 달라 폴리싱이 실물엔 닿지 않음.
- **판정 요청 (로드맵 '기본기 회귀 시 폐기' 규정)** — 두 읽기:
  ① 관문 1(기본기 핵심) 기준: 0.58→0.53% **개선** → 3a 수용 가능.
  ② 관문 2 실물 기준: 19.64→21.86% (-2.2%p) → 규정대로면 폐기 후 재시도
     (다음 레시피 후보: 구·신 코퍼스 혼합 학습, 또는 photo 전용 에폭 교차).
- 셋잇단 성과는 확정적(라인 NER 0.48%·무오류 91.5%) — 폐기해도 코퍼스·코드·이식 장치는 재사용.
- **사장님 판정 대기: ①수용(3b 채택) / ②폐기·재시도.** 판정 전 3b(도돌이) 착수 보류.

### 관문 3a ⑤ — 판정 ②폐기·재시도: 구·신 혼합 학습 (2026-09-23 낮)

- **사장님 판정: ② 폐기·재시도** — 회귀 검사 규칙(실물 성능 후퇴 금지)에 따라 3a/3b 모델 폐기,
  구·신 코퍼스 혼합 학습으로 재학습. 회귀가 사라지면 채택 후 관문 3b(도돌이) 논스톱,
  남으면 정지 후 두 모델 성적표 병렬 보고.
- **혼합 설계 전 겹침 실측(중요)**: `omr_lines`(43,523줄)의 **전 행이 `omr_lines3a` 에
  토큰·분할까지 동일하게 포함**(차집합 0, 토큰 상이 0, split 상이 0). 3a 코퍼스는 순수 확장판
  (+셋잇단 7,294줄)이므로 '혼합'의 실효는 **구 분포 2:1 과표집** — 학습 내 셋잇단 비중
  14.4%→7.7% 희석. 회귀 가설(셋잇단 길이 토큰이 실물의 길이 오독을 증폭)에 정확히 대응하는 처방.
- 구현: `train.py --data` 가 쉼표로 복수 코퍼스 수용, 줄마다 `_root` 를 달아 이미지 경로 해석
  (`dataset.Lines`). 겹치는 줄은 그 수만큼 과표집(의도). 곡 단위 분할은 각 코퍼스 것 그대로
  (SHA1 결정적이라 두 코퍼스 버킷 동일 — 누수 없음 확인).
- 학습 `omr_model_mix` (12:58 시작): 혼합 78,078줄/에폭, 레시피는 3a 와 동일
  (g4 best --init-partial: 공유 1,123행 이식+신규 95행, photo-aug 0.5·s 1.25, lr 1e-4),
  에폭만 5로 조정(78k×5 ≈ 3a 의 45k×8 표본 노출량). 감독기 `omr_train_mix.ps1`.
- 완료 시 자동 평가 6종: 셋잇단(3a 시험셋)·관문1(구데이터)·캐논 실물 auto/켬/끔·모의 v2.
  판단 기준: 캐논 auto ≤ 19.6~20.0%(g4 동률권)면 회귀 소멸 → 채택·3b 진행.

### 관문 3a ⑥ — 혼합 학습 결과: 실물 회귀 미회복(23.99%), '셋잇단 비중' 가설 기각

- 학습(12:58~17:24, 5에폭, 크래시 자동복구 1회): best=ep2, **사진 검증 0.95% 신기록**(3b 1.18%).
- 평가 6종 (omr_model_mix best=ep2):
  | 항목 | 성적 | 기준/비교 |
  |---|---|---|
  | 셋잇단 시험셋 | 0.56% (무오류 90.0%) | ≤5% 통과 (3a 0.51%) |
  | 관문 1 (구데이터) | 0.57% (엘리제 22/27) | 통과, g4 0.58% 동률권 |
  | **캐논 실물 auto** | **23.99%** (켬 21/끔 32) | **g4 19.64%, 3b 21.86% 보다 악화** |
  | 캐논 켬 / 끔 | 34.88% / 27.62% | (g2 시절 켬 38.4% 수준으로 후퇴) |
  | 모의 v2 | **6.75%** (누락 15·유령 3) | ≤10% 통과, **역대 최고**(g4 8.89%) |
- **판정: '셋잇단 비중' 가설 기각.** 셋잇단을 14.4→7.7%로 희석했는데 실물이 더 나빠짐.
  합성 분포 전 지표(사진검증·모의)는 신기록인데 실물만 악화 — **회귀의 본질은 우리 증강
  분포와 실물 캐논 분포의 간극**이고, g4 의 실물 강점은 재학습 총량이 늘수록 씻겨나감.
- 사장님 다음 가설 지시: **g4 강점 = g3→g4 미세조정 체인**(7e-5·6ep → 5e-5·5ep) →
  3b best 에서 같은 체인 재적용(`omr_train_chain.ps1`, 17:52 시작, ch1→ch2).
  코퍼스는 omr_lines3a 유지(구데이터만 쓰면 셋잇단 토큰 95행 억압 위험), 어휘 3b 고정.
  완료 시 4종 평가(셋잇단·관문1·캐논 auto·모의) 자동. 판단 기준: 캐논 19.6~20.0% 회복
  + 셋잇단·관문1 유지 → 채택·관문 3b 논스톱 / 실패 → 정지, g4·3b·혼합·체인 성적표 병렬 보고.

### 관문 3a ⑦ — 체인 재적용 결과: 실물 22.30% 미회복. 정지·성적표 보고 (23:22)

- 학습(17:52~23:06): ch1 6에폭(best=ep3, 사진검증 1.28%) → ch2 5에폭(best=ep0,
  **사진검증 0.68% 신기록**). 크래시 자동복구 정상 동작.
- **전 모델 성적표 (캐논 실물은 --auto, 53장 23,108음):**
  | 항목 | g4(현행) | 3a | 3b | 혼합 mix | 체인 ch2 |
  |---|---|---|---|---|---|
  | 셋잇단 시험셋 (≤5%) | 불가(구어휘) | 0.51% | 0.51% | 0.56% | 0.52% |
  | 관문 1 (구데이터) | 0.58% | 0.53% | 0.53% | 0.57% | **0.53%** |
  | 엘리제 무오류 | 22/27 | 22/27 | 22/27 | 22/27 | 20/27 |
  | **캐논 실물 auto** | **19.64%** | 21.85% | 21.86% | 23.99% | 22.30% |
  | 모의 v2 (≤10%) | 8.89% | 미측정 | 미측정 | 6.75% | **6.64%** |
  | 사진 검증(합성) | 1.31% | — | 1.18% | 0.95% | 0.68% |
- **'미세조정 체인' 가설도 기각.** 두 실험을 관통하는 사실: **합성 지표(사진검증·모의)는
  재학습마다 신기록(1.18→0.95→0.68% / 8.89→6.75→6.64%)인데 실물 캐논만 21.9~24.0%
  에서 안 내려옴.** 어떤 레시피든 신어휘 재학습 총량이 쌓이면 g4 의 실물 적합이 씻겨나가고,
  우리 증강 분포로는 그걸 되찾을 수 없다 — **회귀의 병목은 레시피가 아니라 '합성 증강 ↔
  실물 분포 간극' 그 자체**라는 결론.
- **다음 후보 (사장님 판정 대기):**
  ① **실물 도메인 데이터로 소량 미세조정** — 캐논 53장은 시험셋이라 학습 사용 금지(오염).
    다른 곡 실물 사진(예: 새로 30~50장 촬영)을 학습용으로 받으면 근본 처방 가능. 권고.
  ② 3b(21.86%, 최소 후퇴) 수용하고 관문 3b 진행 — 실물 -2.2%p 감수, 진행 우선.
  ③ g4 유지 + 셋잇단(관문 3a) 연기 — 실물 최우선, 로드맵 후퇴.
  ④ 이중 모델 운용 — 실물 사진은 g4, 렌더/스캔은 ch2 로 라우팅(운영 복잡도 증가).

## 2026-09-24 (식당 PC 웹 세션) — 실물 학습 사진 준비 (판정: ①확보 + 당분간 ③g4 유지)

- **사장님 판정**: ① 실물 학습 사진 확보 진행 + 그동안 ③ g4 유지. ②·④ 기각.
- **인쇄 악보 15곡 생성** (`gen_print_sheets.py`): 코퍼스 소스(퍼블릭 도메인 lieder·
  thesession 미디, **train 분할만** — 시험 오염 없음, 캐논과 무관)에서 약점 기준 배분:
  셋잇단 4(01~04) / 16분 밀집 3(05~07) / 빽빽 보표(staff 16) 3(08~10) /
  쉼표 2(11~12) / 임시표·먼 조(shift ±6, 겹올림표) 3(13~15). 총 토큰 3,472개.
  - A4 1페이지 자동 맞춤(넘치면 마디 78%씩 축소 재시도), 페이지 제목 `OMR-##` =
    촬영본 자동 매칭 키. 코퍼스와 같은 미디 대조 검증(토큰은 조옮김 후 저장 — 🔴 규칙).
  - 산출: `C:/Users/user/omr_print/01..15.{pdf,png} + truth.json` (커밋 금지) →
    PDF 는 `H:\내 드라이브\OMR실물촬영\인쇄용\` 배포.
- **촬영 조건표** (`촬영조건표_실물학습.md`, H: 사본 `촬영조건표.md`): 조건 5종
  (기본/기울임/그림자/원거리/저조도), 곡당 3장 × 15곡 = **45장**, 곡별 배정표 포함.
  수신 폴더: `H:\내 드라이브\OMR실물촬영\사진\`.
- 다음: 사진 도착 → 페이지 제목 매칭 → 보정 파이프라인으로 줄 추출 → truth 와 정렬해
  실물 학습쌍 구성 → ch2(또는 3b)에서 실물 포함 미세조정 + **실물 검증 기반 best 선택**.

### 실물 코퍼스 구축 + 미세조정 착수 (18:18)

- 사진 46장 도착(`H:\…\OMR실물촬영\사진`, 로컬 `omr_real_shots`). **1차 매칭 17/46**
  — 실패 29장 전원이 EXIF ori=1/3 배치. **원인: 수직 하향 촬영 시 자이로가 방향을
  못 잡아 EXIF 가 제멋대로**(성공 배치는 전부 ori=6). 재촬영 없이 해결:
- `real_pack.py`: ① 렌더 15장을 모델로 읽어 **줄별 정답 역산**(단선율판 build_line_truth,
  108줄, 렌더 오차 대부분 0~1%) ② 사진마다 **4방향 회전 탐색**(보표 잡히는 방향만 디코드,
  0/180° 모호성은 매칭 NER 로 해소) × 보정 켬/끔 → 15곡 전역 편집거리 자동 매칭
  ③ 크롭 수=줄 수면 순서 1:1, 아니면 줄 DP(align_lines) 짝짓기 ④ 정규화 크롭 저장.
- **최종 매칭 43/46장** — 학습 247줄 / 검증 63줄(`omr_real_lines`, real:true, 검증
  시트 04·07·14 통째 val). 매칭 NER 분포: 기본조건 0~3.5%, 극한조건 5~45%.
  **실패 3장**(재촬영 후보): 174524·174539·174638 — 짙은 그림자+손떨림 복합으로
  전 방향 보표 0개(시퀀스상 10번 1장·13번 2장 추정).
- 학습 장치 2건: `dataset.py` real 줄은 사진 증강 이중 적용 금지(캐시 경로),
  `train.py` **검증셋에 real 줄이 있으면 그 NER 로 best.pt 선택**(중복 제거 포함) —
  합성 검증이 실물을 예측 못하는 문제의 근본 처방.
- 미세조정 `omr_model_real` 시작(18:18, `omr_train_real.ps1`): ch2 best 에서 lr 5e-5·
  4에폭, 데이터 omr_lines3a + 실물×12 과표집(44,940줄/에폭), 어휘 ch2 고정.
  완료 시 4종 평가(셋잇단·관문1·캐논 auto·모의) 자동.

### 실물 미세조정 결과 — **캐논 19.64 → 10.36%, 실물 간극 돌파** (20:40)

- 학습(18:18~20:23, 4에폭): 실물 검증 NER 3.61→3.14→**2.72%(best=ep2)**→2.72%.
- **평가 4종 (omr_model_real best=ep2):**
  | 항목 | 성적 | 직전 최고 대비 |
  |---|---|---|
  | 셋잇단 시험셋 | **0.50%** (무오류 90.8%) | 3a/3b 0.51% 경신 |
  | 관문 1 | 0.53% (엘리제 **23/27**) | g4 0.58%·22/27 개선 |
  | **캐논 실물 auto** | **10.36%** / **중앙값 4.36%** / **≤10% 37장·≤15% 41장** | **g4 19.64%/11.01%/24장의 절반** |
  | 모의 v2 | **6.49%** (누락 15·유령 3) | 역대 최고 |
- **결론: '합성 증강 ↔ 실물 분포 간극' 진단이 정확했다.** 다른 곡 실물 247줄만으로
  캐논(학습에 전혀 안 쓴 별도 실물 시험셋)이 절반으로 — 실물 도메인 전이 성공.
  best 선택도 실물 검증(2.72%)이 캐논 개선을 정확히 예측 — 선택 신호 문제도 해소.
- 관문 2 기준(전체 ≤10%) 은 0.36%p 차로 아직 미달(최악 사진 5장 37~70%가 지배).
  추가 여지: 실패 3장 재촬영 흡수, 2차 촬영 확대, 극한 사진 유형 대응.
- **판정(사장님 규칙 적용)**: 체인 실험 때 제시된 기준 "캐논 19.6~20.0% 회복 + 셋잇단·
  관문1 유지 → 채택·관문 3b 논스톱"을 크게 상회 — **omr_model_real 을 현행 모델로 채택**,
  g4 는 예비로 보존. 로드맵 순서대로 **관문 3b(도돌이표·볼타) 착수**.

### 관문 3b ① — 구조 토큰·조판 주입 (20:49 코퍼스 재생성 시작)

- **토큰 4종 추가**(lib_lines): 음고 자리 음수 코드 — REP_START(-1)=`|:`,
  REP_END(-2)=`:|`, VOLTA1(-3)=1번괄호, VOLTA2(-4)=2번괄호. 조옮김·미디 대조·
  미디 쓰기(evaluate.write_midi)에서 모두 제외 처리.
- **조판 주입**(make_lib_lines): 줄(청크) 안에서만, 결정적 해시로 ~35% 줄에
  `\repeat volta 2`(± `\alternative` 볼타 1·2 각 1마디) 주입. i0≥1 강제(줄 머리
  반복은 `|:` 가 안 찍혀 라벨-그림 불일치). **경계 붙임줄이 걸리면 주입 포기**
  (전개 시 2회째 연주가 다른 음으로 이어지는 의미 붕괴 방지).
- **검증 강화**: 비전개 미디는 도돌이를 기보 순서대로 연주하지 않음을 실측(4/90
  대조불일치) → 미디를 `\unfoldRepeats` 로 전개해 뽑고, 우리 토큰도
  `unfold_tokens`(pre+body+a1+body+a2 전개)로 펴서 대조 — **구조 기호의 연주
  의미까지 렌더마다 검증**된다. 재시험 90/90 통과, 주입률 36%, 조판 눈검수 정상.
- 코퍼스 `omr_lines3b` 재생성 시작(gen→split→cache, `omr_gen_3b.ps1`) — 밤샘 예상.
  이후: --init-partial(omr_model_real, 신규 4행) + 실물 코퍼스 유지 학습 →
  판정(구조 토큰 정확도 ≥95%, 전개 미디 일치 곡 ≥90%) + 회귀 4종.

### 관문 3b ③ — 학습·판정 결과: 구조 완벽 + **관문 2 최초 통과 (8.92%)** (03:34)

- 코퍼스 완성: 렌더 50,618(대조불일치 209 자동 탈락)·캐시 50,607줄. 학습
  `omr_model_3b2`(00:18~03:17, 6에폭, 실물 코퍼스 유지): 실물 검증
  3.76→…→**2.61% 신기록**(best=ep5).
- **관문 3b 판정** (`eval_structure`, 구조 줄 1,274·218곡):
  | 지표 | 성적 | 기준 |
  |---|---|---|
  | 구조열 줄 일치 | **99.76%** | ≥95% **통과** |
  | 구조 토큰 정밀/재현 | 99.95% / 99.95% | — |
  | 전개 연주 줄 일치 | 96.62% | — |
  | **전개 연주 곡 일치** | **87.16%** (190/218) | ≥90% **미달 -2.8%p** |
  - 분해: 구조열 불일치 줄은 1,274 중 **3줄뿐** — 전개 불일치 43줄의 40줄은
    도돌이 오독이 아니라 **일반 음표 오독**이 전개 비교에 걸린 것. 곡 단위
    '구조 줄 전부 무오류' 기준이라 곡 길이에 민감(구조 원인 실패는 최대 3곡).
- **회귀 전 종목 (omr_model_3b2):** 3b 전체 0.49% / 셋잇단(3a셋) 0.52% /
  관문 1 0.55%(엘리제 22/27) / 모의 6.49% / **캐논 실물 auto 8.92%·중앙값
  2.98%·≤10% 40/53장 — `gate2_real_pass: True`, 관문 2 기준(≤10%) 최초 통과.**
  (실물 처방 이후 두 번의 학습이 연달아 캐논을 개선: 19.64→10.36→8.92%.)
- **사장님 판정 요청**: 관문 3b 전개 곡 87.2% vs 90%.
  ① 통과 인정 — 구조 인식 자체(3b 의 목적)는 99.8~100% 완벽, 미달분은 일반
    음표 무오류율 문제로 기존 NER 지표가 관장하는 영역이라는 읽기. 3c 진행.
  ② 미달 처리 — 폴리싱 1~2에폭 or 재도전으로 90% 돌파 후 3c.
  어느 쪽이든 **omr_model_3b2 채택 권고**(캐논·실물검증·모의 전부 역대 최고,
  관문 1 0.55% 기준 내). 판정 전 3c(빠르기·셈여림) 착수 보류.
